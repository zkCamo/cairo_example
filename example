// Создает новый токен
let token = create(id: 1, amount: 100);

// Проверяет количество токенов у владельца
assert(balance(1) == 100);

// Переводит 50 токенов от одного владельца к другому
transfer(1, 2, 50);

// Проверяет количество токенов у владельца
assert(balance(1) == 50);
